.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install pygmm

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv pygmm
    $ pip install pygmm
